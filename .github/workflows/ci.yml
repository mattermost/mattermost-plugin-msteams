name: ci
on:
  push:
    branches:
      - main
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
  pull_request:

permissions:
  contents: read

jobs:
  plugin-ci:
    uses: mattermost/actions-workflows/.github/workflows/plugin-ci.yml@19484070ed69e3f6b4315c679c43e5fac439a8f9
    with:
      golangci-lint-version: "v1.54.2"
      golang-version: "1.21"
    secrets: inherit
